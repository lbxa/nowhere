{
  "name": "nowhere",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.2.19",
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "lint": "bun --filter '*' lint",
    "preview": "bun --filter '*' preview"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "libs/*"
  ]
}
